# Админ-панель

## Главные страницы

- `admin/index.php` — список кампаний и главная статистика
- `admin/campsettings.php` — настройки кампании
- `admin/statistics.php` — статистические таблицы кампании
- `admin/clicks.php` — просмотр кликов
- `admin/database.php` — очистка статистики и обслуживание SQLite

## Dashboard

На главной странице доступны:

- список кампаний
- создание кампании
- trafficback URL
- экспорт таблицы
- настройка набора колонок
- внутренняя прокрутка списка кампаний при заполнении доступной высоты окна
- индикаторы состояния в панели действий над таблицей: свободное место на диске, размер SQLite-базы, каталога кэша и логов

![Dashboard со списком кампаний](../assets/screenshots/admin-dashboard-campaigns.png)

Размер **DB** учитывает основной файл SQLite и его служебные WAL/SHM-файлы. Нажатие на индикатор открывает страницу обслуживания базы. **Cache** показывает весь настроенный каталог кэша, включая загруженные лендинги и safe pages. Значения пересчитываются не чаще одного раза в минуту, чтобы большие каталоги не замедляли открытие Dashboard. При остатке диска менее 15% индикатор становится жёлтым, менее 5% — красным.

Кнопка **Settings** открывает [системные настройки](system-settings.md), управление плагинами и обновлениями. Индикатор **Logs** открывает [просмотрщик серверных журналов](testing-and-diagnostics.md#серверные-журналы). Дата GeoBases в header носит информационный характер.

## Удаление кликов

Страница **DB** показывает кампании с количеством кликов и заблокированных записей за выбранный в header диапазон и за всё время. Trafficback находится в последней строке той же таблицы и отмечен как **GLOBAL**.

Можно выбрать одну или несколько строк, при этом **Select all** включает Trafficback. Удаление затрагивает клики, lead/purchase-статусы, payout/cost, историю конверсий, связанные шаги с их event-данными, а также соответствующие blocked-записи. Настройки кампаний, `common`, файлы, кэш и серверные логи сохраняются.

Перед началом YellowTDS повторно считает выбранные записи и предупреждает о свежем трафике. Автоматический бэкап не создаётся: для подтверждения необратимой операции нужно ввести `DELETE`. Записи удаляются порциями по 1000 кликов; закрытие страницы останавливает процесс между порциями, после повторного открытия его можно продолжить. Новые клики, пришедшие после запуска, в текущую операцию не включаются.

После удаления YellowTDS автоматически выполняет checkpoint и `VACUUM`, затем показывает размер базы до и после. Во время VACUUM запись статистики может быть заблокирована, поэтому обслуживание лучше запускать при низком трафике. Если свободного места недостаточно или компактизация прервана, удаление остаётся завершённым, а **Retry compaction** позволяет повторить только VACUUM. Кнопка **Compact database** запускает компактизацию без удаления статистики.

![Обслуживание базы данных](../assets/screenshots/database-maintenance.png)

## Что хранится в UI

Админка управляет:

- columns layouts
- campaign settings JSON
- statistics tables
- filters and order by rules

## File management

Через админку можно:

- выбирать папки safe pages и лендингов
- редактировать файлы
- загружать ZIP

Кнопка **Add Existing** открывает окно с поиском и чекбоксами: за один раз можно отметить и добавить несколько папок. Выбор сохраняется при фильтрации списка. В длинных списках прокручивается только область папок, а поиск и кнопки **Cancel** / **Add selected** остаются на месте.

Названия выбранных папок отображаются как неизменяемые идентификаторы и не получают фокус мышью или клавишей Tab. Поля redirect, CURL и HTTP-кодов остаются редактируемыми.
